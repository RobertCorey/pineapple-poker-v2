{
  "name": "pineapple-poker",
  "private": true,
  "workspaces": [
    "frontend",
    "functions",
    "dealer"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "lint": "npm run lint -w frontend",
    "dealer": "npm run dev -w dealer",
    "dealer:build": "npm run build -w dealer",
    "functions:watch": "npm run build:watch -w functions",
    "dev:up": "./scripts/dev-up.sh",
    "dev:down": "./scripts/dev-down.sh",
    "test": "npx playwright test --ignore='**/bot.spec.ts'",
    "test:unit": "vitest run --config shared/vitest.config.ts && npm test -w dealer -- --run src/dealer.test.ts",
    "test:dealer": "npm test -w dealer",
    "test:dealer:unit": "npm run test:unit -w dealer",
    "test:dealer:integration": "npm run test:integration -w dealer"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vitest": "^4.0.18"
  }
}
