{
  "name": "pineapple-dealer",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "dev": "tsx --watch src/index.ts",
    "test": "vitest run --config vitest.config.ts",
    "test:unit": "vitest run --config vitest.config.ts src/dealer.test.ts",
    "test:integration": "vitest run --config vitest.config.ts src/game-engine-guards.test.ts"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "~5.7.0",
    "vitest": "^4.0.18"
  }
}
